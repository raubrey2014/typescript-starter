name: Starter CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Install dependencies'
        run: npm install
      - name: 'Lint'
        run: npm run lint
      - name: 'Test'
        run: npm run test